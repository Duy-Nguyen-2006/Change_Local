# Comprehensive Instructions for Running All Crawler Files

## Overview
This document provides detailed instructions for running crawler scripts in different operating systems, including dependencies installation and examples for each crawler type.

## Windows-only Files (/Crawl/win_code/)
### Setup Instructions
1. Ensure you have Python installed. You can download it from [python.org](https://www.python.org/).
2. Install required dependencies:
   ```bash
   pip install -r requirements.txt
   ```

### How to Run Each Script
- **Example Script 1**: To run `script_1.py`:
  ```bash
  python script_1.py
  ```

## Linux-only Files (/Crawl/linux_code/)
### Setup Instructions
1. Ensure you have Python installed. You can download it from [python.org](https://www.python.org/).
2. Install required dependencies:
   ```bash
   pip install -r requirements.txt
   ```

### How to Run Each Script
- **Example Script 2**: To run `script_2.py`:
  ```bash
  python script_2.py
  ```

## Crawl4AI Files
### Setup Instructions
1. Ensure you have Python 3.9 or later installed.
2. Install required dependencies:
   ```bash
   pip install -r crawl4ai_requirements.txt
   ```

### How to Run Each Script
- **Example Crawl4AI Script**: To run `crawl4ai.py`:
  ```bash
  python crawl4ai.py
  ```

## Facebook Crawler
### Setup Instructions
1. Ensure you have Python installed and required libraries.
2. Install dependencies using:
   ```bash
   pip install -r facebook_crawler_requirements.txt
   ```

### How to Run each script
- **Example Facebook Crawler**: To run `facebook_crawler.py`:
  ```bash
  python facebook_crawler.py
  ```

## OS Compatibility
| OS         | Supported Scripts                                    |
|------------|-----------------------------------------------------|
| Windows    | All scripts in `win_code/`                          |
| Linux      | All scripts in `linux_code/`                        |
| macOS      | Crawl4AI and Facebook Crawler                        |

### Troubleshooting
- Ensure all dependencies are installed correctly.
- If you encounter errors, please refer to the documentation for further debugging steps.

### Conclusion
Follow the above sections to set up and run each of the crawler files according to your operating system's requirements.